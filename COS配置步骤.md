# 欲言信箱 —— 腾讯云 COS 对象存储配置步骤

> 本方案用腾讯云 COS 对象存储作为共享后端，零成本、不需要数据库密码、不依赖外网/内网地址。后端程序通过内网直连 COS，手机/电脑/不同浏览器都能看到彼此发布的内容。

## 第一步：创建 COS 存储桶

1. 进入 **腾讯云控制台** → 搜索「对象存储 COS」→ 进入。
2. 左侧点击 **「存储桶列表」** → **「创建存储桶」**。
3. 填写：
   - **名称**：自定义英文名（如 `yuyan`），系统会自动加一个数字后缀（APPID），最终桶名类似 `yuyan-1251234567`。**请记下完整桶名**（后面环境变量要用）。
   - **地域**：选和云托管相同的地域（通常是 **广州 / ap-guangzhou**；如果你的云托管服务在别的地域，就选那个地域）。
   - **访问权限**：选 **私有读写**（数据安全，只有用密钥才能读写）。
4. 其他保持默认，点「完成创建」。

## 第二步：获取 API 密钥

1. 进入 **腾讯云控制台** → 右上角头像 → **「访问管理」** → 左侧 **「API 密钥管理」**（或访问管理 → 用户 → 自己 → API密钥）。
2. 看到一组密钥：
   - **SecretId**（例如 `AKIDxxxxxxxxxxxxxxxx`）
   - **SecretKey**（一长串，创建时显示一次，若忘了需「新建密钥」或「显示」）
3. 复制这两个值（后面填环境变量用）。

> ⚠️ SecretKey 等同账号密码，**不要截图发给别人**，只填进云托管环境变量即可。

## 第三步：配置云托管环境变量

1. 进入 **CloudBase 云托管** → 服务 `yuyan` → **「服务设置」/「环境变量」**。
2. 新增 / 修改 4 个变量（注意是**新增同名变量**，不要拼错大小写）：
   - `COS_SECRET_ID` = 第二步复制的 SecretId
   - `COS_SECRET_KEY` = 第二步复制的 SecretKey
   - `COS_BUCKET` = 完整桶名（如 `yuyan-1251234567`）
   - `COS_REGION` = 地域代码（如 `ap-guangzhou`）
3. **删除旧变量（非常重要，否则会干扰）**：
   - `STORAGE`（旧值可能是 `tcb`，会让诊断里出现 `EXPLICIT_STORAGE:"tcb"`，必须删掉）
   - `DATABASE_URL`、`MONGODB_URI`、`LEANCLOUD_APP_ID`、`LEANCLOUD_APP_KEY`、`TENCENTCLOUD_SECRETID`、`TENCENTCLOUD_SECRETKEY`
4. **保存**。

> 🔴 **关键提醒：在 CloudBase 里，改完环境变量后必须「重新部署」才会生效。** 只点「保存」、不重新部署，容器里读到的还是旧变量（这正是上次 `/api/debug` 显示 `mode:"file"` 的原因）。

## 第四步：重新部署（让新变量生效）

1. 在 CloudBase 云托管控制台，找到服务 `yuyan`。
2. 点击 **「重新部署」**（从 GitHub 最新提交构建）。
3. 等待构建完成，状态变「运行中」。

## 第五步：验证连通

1. 浏览器访问：
   `https://yuyan-294871-10-1341046552.sh.run.tcloudbase.com/api/debug`

2. **成功的话**，返回里应当有 `mode:"cos"` 且 `cos.ok:true`：
```json
{
  "mode": "cos",
  "cosConfigured": true,
  "cos": { "ok": true, "questionCount": 0, "replyCount": 0 }
}
```

3. **如果还是 `mode:"file"`**（带 `cosConfigured:false`），说明 4 个 COS 变量至少有一个没配进去，或部署后没生效：
   - 检查 `cosConfigured` 值：为 `false` → 变量没配齐/没生效；为 `true` 但仍 `mode:file` → 没重新部署。
   - 回到第三步确认 4 个变量名、值都没拼错，再走第四步重新部署一次。

4. 如果 `cos.ok` 为 `false`，把返回的 `message` 复制发我。

## 第六步：两浏览器互测

1. 电脑浏览器发一条测试问题。
2. 手机浏览器打开同一地址，确认能看到。
3. 看不到就把 `/api/debug` 结果和截图发我。

## 常见问题

**Q：数据存在哪里？**
A：一个 COS 对象（默认 `yuyan/data.json`），里面是全部问题+回复+账号。后端每次读写都下载/覆盖它。数据量极小（KB 级），请求费可忽略。

**Q：COS 要花钱吗？**
A：新用户有免费额度（标准存储 50GB 等），信箱数据极小，基本零成本。同地域访问走腾讯云内网，免流量费。

**Q：需要配 CORS 吗？**
A：不需要。前端只访问我们的云托管 API，后端再去访问 COS，前端不直接连 COS。

**Q：之前发过的内容还在吗？**
A：不在了。之前各种存储后端都没真正连通，数据写在容器临时文件里，重部署就清空。配置好 COS 后请重新发布历史内容。
